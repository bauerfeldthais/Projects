
/**
 * Receive an array with students and its grades, instantiate new Student objects and return it
 * 
 * @param {array[]} grades
 * 
 * @returns {Student[]} 
 */
export function transformStudents(grades) {
    // converer dadso para instancias de Student
    // dividir notas por alunos e classes

}